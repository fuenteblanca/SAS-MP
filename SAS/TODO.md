# TODO

- [ ] Replace unsupported File/Paths/FileReader usage in `app/(tabs)/payslip.tsx` with Expo FileSystem-compatible download/export flow.
- [ ] Apply the same export refactor to `app/(tabs)/ddo-ao.tsx` and ensure it no longer crashes on download.
- [ ] Re-test geofence and notification scheduling after the export fixes to confirm no regressions.
